<div class="container my-5">
  <div class="row justify-content-center">
    <div class="col-12 col-md-8">

      <h1 class="display-4 text-success mb-4">夜ごはんの投稿をチェック</h1>

      <div class="card shadow-sm mb-4">
        <% if @dinner.image.attached? %>
          <%= image_tag @dinner.image.variant(resize_to_limit: [600, 400]), class: "card-img-top rounded" %>
        <% end %>
        <div class="card-body">
          <h2 class="card-title font-weight-bold"><%= @dinner.title %></h2>
          <p class="card-text text-muted"><%= simple_format(@dinner.body) %></p>

          <% if @dinner.recipes.any? %>
            <div class="mt-3">
              <h5>この夜ごはんに使ったレシピ：</h5>
              <ul class="list-group list-group-flush">
                <% @dinner.recipes.each do |recipe| %>
                  <li class="list-group-item p-1">
                    <%= link_to recipe.title, recipe_path(recipe), class: "text-success" %>
                  </li>
                <% end %>
              </ul>
            </div>
          <% end %>

          <p class="mt-4 text-muted small">
            投稿者：
            <% if current_user == @dinner.user %>
              <%= link_to @dinner.user.username, user_path(current_user), class: "text-decoration-none" %>
            <% else %>
              <%= link_to @dinner.user.username, user_path(@dinner.user), class: "text-decoration-none" %>
            <% end %>
            <br>
            投稿日：<%= @dinner.created_at.strftime("%Y年%m月%d日") %>
          </p>
        </div>
      </div>

      <div class="d-flex justify-content-between flex-wrap mr-2">
        <% if current_user == @dinner.user %>
          <%= link_to '編集する', edit_dinner_path(@dinner), class: 'btn btn-outline-success flex-grow-1 flex-md-grow-0' %>
          <%= link_to '削除する', dinner_path(@dinner), method: :delete, data: { confirm: '本当に削除してもよろしいですか？' }, class: 'btn btn-outline-danger flex-grow-1 flex-md-grow-0' %>
        <% end %>
        <%= link_to '投稿一覧へ戻る', dinners_path, class: 'btn btn-secondary flex-grow-1 flex-md-grow-0' %>
      </div>

    </div>
  </div>
</div>
